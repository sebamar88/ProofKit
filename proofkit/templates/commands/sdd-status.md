Show the status of the active ProofKit change and tell me exactly what to do next.

1. Run `proofkit run <change_id>` (or `proofkit status` if you don't know the ID).
2. Read the output and tell me: current phase, which artifact is waiting, and the single next action.
3. If the change is blocked, explain why in one sentence.
